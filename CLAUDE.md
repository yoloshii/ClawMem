# ClawMem — Claude Code

Claude Code reads `CLAUDE.md` natively (not `AGENTS.md`), so this file imports the root SSOT:

@AGENTS.md

All agent-facing guidance lives in [AGENTS.md](AGENTS.md) (the lean quick reference) and `docs/` (deep reference, indexed at the foot of AGENTS.md). `SKILL.md` is the portable on-demand operating reference for using ClawMem from other projects.
